
✅ 一、安装依赖
bash
运行
npm install vue-i18n@9
✅ 二、创建语言文件
1. src/lang/zh.js（中文）
js
export default {
  login: {
    title: '后台管理系统',
    username: '用户名',
    password: '密码',
    loginBtn: '登录'
  },
  menu: {
    home: '首页',
    user: '用户管理',
    role: '角色管理',
    setting: '系统设置'
  },
  common: {
    confirm: '确定',
    cancel: '取消',
    delete: '删除',
    edit: '编辑'
  }
}
2. src/lang/en.js（英文）
js
export default {
  login: {
    title: 'Admin System',
    username: 'Username',
    password: 'Password',
    loginBtn: 'Login'
  },
  menu: {
    home: 'Home',
    user: 'User Management',
    role: 'Role Management',
    setting: 'Settings'
  },
  common: {
    confirm: 'Confirm',
    cancel: 'Cancel',
    delete: 'Delete',
    edit: 'Edit'
  }
}
✅ 三、创建 i18n 实例（核心）
src/lang/index.js
js
import { createI18n } from 'vue-i18n'
import zh from './zh'
import en from './en'

// 从本地存储获取语言，默认中文
const savedLang = localStorage.getItem('lang') || 'zh'

const i18n = createI18n({
  legacy: false, // Vue3 必须写
  locale: savedLang,
  messages: {
    zh,
    en
  }
})

export default i18n
✅ 四、在 main.js 全局引入
js
import { createApp } from 'vue'
import App from './App.vue'
import i18n from './lang'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import localeZH from 'element-plus/es/locale/lang/zh-cn'
import localeEN from 'element-plus/es/locale/lang/en'

const app = createApp(App)

app.use(i18n)
app.use(ElementPlus, {
  locale: i18n.global.locale.value === 'zh' ? localeZH : localeEN
})

app.mount('#app')
✅ 五、语言切换组件（直接放顶部导航）
任何 Vue 组件里（Navbar.vue）
vue
<template>
  <el-dropdown @command="handleLangChange">
    <span class="el-dropdown-link">
      {{ currentLang === 'zh' ? '中文' : 'English' }}
    </span>
    <template #dropdown>
      <el-dropdown-menu>
        <el-dropdown-item command="zh">中文</el-dropdown-item>
        <el-dropdown-item command="en">English</el-dropdown-item>
      </el-dropdown-menu>
    </template>
  </el-dropdown>
</template>

<script setup>
import { ref, watch } from 'vue'
import { useI18n } from 'vue-i18n'
import { ElMessage } from 'element-plus'

const { locale } = useI18n()
const currentLang = ref(locale.value)

// 切换语言
const handleLangChange = (lang) => {
  currentLang.value = lang
  locale.value = lang
  localStorage.setItem('lang', lang)
  ElMessage.success(lang === 'zh' ? '切换为中文' : 'Switched to English')
}
</script>
✅ 六、页面中如何使用多语言？
vue
<template>
  <h1>{{ $t('login.title') }}</h1>
  <el-input :placeholder="$t('login.username')" />
  <el-button>{{ $t('common.confirm') }}</el-button>
</template>